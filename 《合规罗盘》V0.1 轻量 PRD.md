# 《合规罗盘》V0.1 轻量 PRD

## 1. 文档信息

项目名称：合规罗盘

文档版本：V0.1

当前阶段：0-1 首版定义

产品目标版本：V0

适用对象：产品、研发、设计、测试

## 2. 产品目标

### 2.1 本期产品目标

V0 版本目标：

让用户可以快速上传合同并获得 AI 风险初筛结果。

让用户可以在 Web 工作台中查看风险点高亮、理解风险原因，并进行人工确认。

让用户可以上传两个版本的合同进行差异比对，识别重点改动。

形成“后端结构化解析 + Coze AI 分析 + 用户人工确认”的首版闭环。

### 2.2 本期不做

本期暂不纳入开发范围：

独立规则库管理后台

历史合同逆向建模生成私有规则

企业背景自动注入（如企信数据动态拉取）

多模型复杂路由策略可视化配置

自动审批流转 / OA 审批打通

付费订阅、计费、结算系统

完整权限体系与多组织管理

## 3. 用户角色

### 3.1 普通使用者

身份：

业务人员

财务人员

基层审核人员

核心目标：

快速知道合同有没有明显风险

快速定位问题位置

快速看懂版本改动

降低逐字阅读成本

### 3.2 管理/审核责任人

身份：

财务负责人

业务负责人

管理者

核心目标：

对关键风险点进行最终确认

保证 AI 建议不会直接替代人工决策

留下审查结果和处理记录

## 4. 产品范围与优先级概览

### 4.1 本期功能模块总览

@郑樱蕾@飞书用户7504UZ@七元@蒋运霞

Web 上传入口 —— P0

合同上传与解析（后端）—— P0

Coze AI 风险分析接入 —— P0

初筛结果返回模块 —— P0

Web 风险审查工作台 —— P0

版本比对（后端 diff + Coze 语义增强）—— P0

人工确认闭环 —— P0

审查记录列表 —— P1

结果导出 —— P1

IM 接入与结果卡片 —— P1

IM 深度协同（@人、转发、追问）—— P2

### 4.2 首版接入策略

首版采用以下策略：

首版实现入口：Web 上传页

目标产品形态：IM 轻入口 + Web 深审工作台

首版先验证核心能力链路，暂不以 IM 接入作为阻塞项

后续接入 IM 时，上传与初筛结果返回可迁移为 IM 对话内完成，Web 继续承担深度审查与版本比对

## 5. 核心流程

@郑樱蕾@飞书用户7504UZ@七元@蒋运霞

### 5.1 流程 A：单份合同审查

用户上传一份合同文件

- → 后端接收文件
- → 后端完成解析、脱敏、结构化处理
- → 后端将结构化内容传给 Coze 工作流 API
- → Coze 返回风险分析结果
- → 后端封装风险简报
- → 返回初筛结果
- → 用户决定是否进入 Web 深度审查

### 5.2 流程 B：Web 深度审查

用户进入 Web 工作台 → 查看合同正文 → 风险位置高亮展示 → 点击风险点查看原因与建议 → 对建议执行“确认 / 忽略”。

### 5.3 流程 C：版本比对

用户上传两个版本合同

- → 后端解析两份文档
- → 后端生成基础 diff 结果（新增 / 删除 / 修改）
- → 后端将 diff 结果与合同内容传给 Coze
- → Coze 返回重点改动摘要与风险说明
- → 后端合并结果
- → Web 展示差异高亮与重点改动区域
- → 用户查看并确认风险

## 6. 页面与模块结构

@蒋运霞

### 6.1 页面清单

Web 上传页（P0）

首版文件接入入口，支持单份合同审查与双版本比对任务创建。

初筛结果返回模块（P0）

用于承接单份合同分析后的风险简报。

首版可由 Web 页面承接；后续接入 IM 后，可迁移为 IM 结果卡片。

Web 深度审查工作台（P0）

用于查看合同正文、高亮风险点、查看 AI 建议、执行确认/忽略。

版本比对页（P0）

用于查看双版本差异结果、重点改动说明、风险摘要。

审查记录页（P1）

用于查看历史任务及处理结果。

IM 初筛结果卡片（P1）

目标产品形态之一，用于在企业微信/飞书中展示风险摘要，并引导进入 Web 深审。

### 6.2 页面关系

Web 上传页 → 初筛结果返回模块

初筛结果返回模块 → Web 深度审查工作台

Web 上传页 → 版本比对页

工作台 / 比对页 → 人工确认结果

后续：IM 上传 → IM 结果卡片 → Web 深审工作台

## 7. 功能需求明细

### 7.1 合同上传与解析【P0，后端主责】

@郑樱蕾@飞书用户7504UZ@七元

#### 7.1.1 功能目标

支持用户上传合同文件，后端完成解析、脱敏、结构化输出，为后续 Coze AI 分析和 Web 风险高亮提供标准输入。

#### 7.1.2 责任边界

后端负责：

接收用户上传的原始文件

完成文件格式校验

调用自研解析工具或解析服务

完成脱敏与结构化处理

输出标准化中间结果

Coze 不负责：

原始文件主入口

原始文件长期存储

基础文档解析主逻辑

#### 7.1.3 用户操作

上传单份合同进行审查

上传两份合同进行版本比对

#### 7.1.4 系统输出

后端解析后至少输出：

合同纯文本内容

段落 / 条款结构

可用于定位的位置信息

可用于比对的中间结果

脱敏后的 AI 输入内容

#### 7.1.5 支持范围

建议首版支持：

docx

pdf（如解析能力可控）

txt（可选）

#### 7.1.6 关键规则

单份审查必须上传 1 个文件

版本比对必须上传 2 个文件

文件上传后需展示处理中状态

解析失败不得直接进入 AI 分析阶段

#### 7.1.7 异常处理

文件格式不支持

文件损坏

文件为空

文件过大

解析超时

脱敏失败

#### 7.1.8 验收标准

用户可成功上传文件并进入后续分析流程

后端可输出稳定的结构化结果

失败时有明确提示

单文档和双文档任务能区分创建

### 7.2 Coze AI 风险分析接入【P0】

@郑樱蕾@飞书用户7504UZ@七元

#### 7.2.1 功能目标

将后端产出的结构化合同内容传给 Coze 工作流 API，由 Coze 完成风险分析、摘要生成和结果返回。

#### 7.2.2 Coze 负责范围

风险摘要生成

风险点分类与等级判断

风险原因解释

建议处理方向生成

单份合同初筛结论输出

#### 7.2.3 后端负责范围

组织请求参数

调用 Coze workflow API

校验 Coze 返回结构

标准化结果

将结果封装给 Web / IM

#### 7.2.4 单份合同结果建议字段

Coze 返回结果建议至少包含：

总体结论

风险点列表

风险等级

风险说明

建议处理方向

是否建议进入深审

#### 7.2.5 首版内置检查项示例

合同主体是否明确

金额、日期、期限等关键信息是否前后不一致

付款条款是否缺失或模糊

违约责任条款是否缺失或模糊

自动续约、解约条件是否存在明显风险

权责明显不对等的可疑条款

#### 7.2.6 关键规则

Coze 输出仅做辅助提示，不直接形成审批结论

所有风险点默认“待人工判断”

若 Coze 结果不确定，应返回“建议人工复核”

#### 7.2.7 异常处理

Coze API 调用失败

返回结构异常

返回字段缺失

风险点无法映射原文位置

#### 7.2.8 验收标准

后端可成功调用 Coze 并拿到风险结果

返回结果可被标准化存储与展示

用户可看到清晰的风险简报

### 7.3 初筛结果返回模块【P0】

@飞书用户7504UZ@七元@蒋运霞

#### 7.3.1 功能目标

向用户返回单份合同的风险简报，并引导用户进入后续深度审查。

#### 7.3.2 首版实现方式

首版先采用 Web 承接结果返回。

后续可扩展为 IM 结果卡片。

#### 7.3.3 返回内容

至少包含：

文件名

初筛结论

高/中/低风险数量

重点风险摘要

操作入口：进入 Web 深审

#### 7.3.4 验收标准

用户可在上传后看到简明的风险摘要

用户可一键进入 Web 深审工作台

### 7.4 Web 风险审查工作台【P0】

@飞书用户7504UZ@七元@蒋运霞

#### 7.4.1 功能目标

支持用户在 Web 页面中沉浸式查看合同正文、定位风险点、理解 AI 建议，并进行人工处理。

#### 7.4.2 页面结构建议

左侧：合同正文

右侧：风险列表 / 风险详情

文中高亮：风险位置

详情区：风险原因、建议处理方式、状态

这与立项书中“左右分栏 + 高亮 + 一键处理建议”的方向一致，但首版建议先实现基础形态，不追求过重的复杂交互。

#### 7.4.3 用户操作

浏览合同正文

点击高亮风险点

查看风险解释

查看建议修改方向

对风险点执行“确认 / 忽略”

#### 7.4.4 系统反馈

高亮同步定位

风险详情同步更新

记录用户处理状态

#### 7.4.5 风险点字段建议

每个风险点至少包含：

风险标题

风险等级

风险说明

原文位置

建议处理

当前状态（待处理 / 已确认 / 已忽略）

#### 7.4.6 关键规则

首版不做自动改文

首版不做一键替换回写原文

首版只提供建议文本，不直接修改合同

每个风险点必须可被人工标记状态

#### 7.4.7 异常处理

高亮失败但风险存在

原文定位不精确

风险详情缺失

#### 7.4.8 验收标准

用户可在合同中看到风险高亮

点击风险点能看到说明

用户可以完成“确认/忽略”操作

处理状态可保存到当前审查结果中

### 7.5 版本比对【P0】

@郑樱蕾@飞书用户7504UZ@七元@蒋运霞

#### 7.5.1 功能目标

帮助用户快速识别两个版本合同之间的差异，并结合 AI 判断哪些改动值得重点关注。

#### 7.5.2 责任边界

后端负责：

解析两份合同

生成基础 diff 结果

输出新增 / 删除 / 修改分类

提供差异定位数据

Coze 负责：

对重点改动进行语义解释

输出风险摘要

给出改动关注建议

标记可能的高风险改动区域

#### 7.5.3 用户操作

上传两个版本合同

查看差异结果

查看新增/删除/修改内容

查看重点改动提示

#### 7.5.4 页面展示建议

左右双栏展示两个版本

改动内容高亮

差异列表区展示改动摘要

重点改动单独标记

#### 7.5.5 系统输出

至少包含：

新增内容

删除内容

修改内容

差异摘要

重点改动风险说明

#### 7.5.6 关键规则

用户需明确“旧版本/新版本”

首版优先支持文本级差异

条款级结构化差异可作为增强项

首版由后端产出基础 diff，不依赖 Coze 自由生成差异

#### 7.5.7 异常处理

两份文件无法匹配

文本结构差异过大

Coze 仅返回部分重点说明

差异可定位但 AI 无法给出清晰解释

#### 7.5.8 验收标准

用户可清晰看到两版差异

差异可被定位

后端可输出新增/删除/修改三类结果

Coze 可返回重点改动摘要

### 7.6 人工确认闭环【P0】

@飞书用户7504UZ@七元@蒋运霞

#### 7.6.1 功能目标

确保产品始终是“AI 辅助工具”，不是自动决策工具。

#### 7.6.2 用户操作

对风险点进行：

确认

忽略

#### 7.6.3 系统规则

所有 AI 风险点初始状态为“待处理”

只有用户明确操作后，状态才变为“已确认”或“已忽略”

产品不允许默认自动通过全部风险点

#### 7.6.4 输出结果

一次审查结束后应能形成：

风险点总数

已确认数量

已忽略数量

未处理数量

#### 7.6.5 验收标准

每条风险点均支持状态流转

用户可看到当前处理进度

结果页能反映整体处理情况

立项书中已经明确提出“人在回路”是刚性约束，因此这部分应视作首版核心，不是附加功能。

### 7.7 IM 初筛结果卡片【P1】

ps：待细化设计

@曹莹

它是用户在 IM 对话里看到的首个结果，不是网页。

建议卡片里包含：

文件名

初筛结论（风险较低 / 建议重点审查 / 存在明显异常）

风险数量（高/中/低）

2 到 5 条重点风险摘要

一个操作按钮：进入 Web 深审

一个操作按钮：查看版本比对（如果该任务是比对任务）

### 7.8 审查记录页【P1】

#### 7.8.1 功能目标

让用户可以查看历史审查结果，避免每次只看当前会话。

#### 7.8.2 功能点

展示历史文件名

展示审查时间

展示审查类型（单文档审查 / 版本比对）

展示风险数量

点击查看详情

#### 7.8.3 首版说明

如果开发资源紧张，可先只保留最简列表，不做复杂筛选。

### 7.9 结果导出【P1】

#### 7.9.1 功能目标

支持将审查结果导出，用于内部沟通或存档。

#### 7.9.2 导出内容

文件名称

审查时间

风险列表

风险状态

差异摘要（如为比对任务）

#### 7.9.3 首版说明

可优先支持文本/PDF 导出，样式可简化。

## 8. 优先级清单（按功能点）

### P0 必做

Web 上传页  @蒋运霞  @飞书用户7504UZ@七元

单文档上传    @飞书用户7504UZ@七元

双文档上传    @飞书用户7504UZ@七元

后端文件解析  @飞书用户7504UZ@七元

后端脱敏处理  @飞书用户7504UZ@七元

结构化中间结果输出   @飞书用户7504UZ@七元

Coze workflow API 接入  @郑樱蕾@飞书用户7504UZ@七元

AI 初筛结果输出  @飞书用户7504UZ@七元@郑樱蕾

初筛结果展示模块 @蒋运霞

Web 合同高亮展示 @蒋运霞

风险详情展示 @蒋运霞

风险点确认/忽略 @蒋运霞 @七元@飞书用户7504UZ

后端基础 diff @飞书用户7504UZ@七元

差异分类（新增/删除/修改） @飞书用户7504UZ@七元

Coze 重点改动摘要 @郑樱蕾

审查结果状态统计（此处对应【7.6 人工确认闭环】部分的需求） @蒋运霞 @飞书用户7504UZ@七元

### P1 应做

IM 文件接入

IM 初筛结果卡片

从 IM 跳转 Web 详情

审查记录页

结果导出

### P2 后续

独立规则页

规则可配置

历史合同逆向建模

企业背景动态注入

自动改写建议回填原文

多模型动态路由增强

## 9. 验收标准（版本级）

V0 版本完成的标志：

用户可完成单份合同上传并拿到风险初筛结果。

后端可完成解析、脱敏、结构化，并成功调用 Coze 工作流。

用户可进入 Web 工作台查看风险高亮并逐条处理。

用户可上传两份合同完成版本比对。

版本比对由后端生成基础 diff，Coze 生成重点改动说明。

所有 AI 风险建议均需人工确认，不自动生效。

首版不依赖独立规则库，也能跑通核心审查流程。

## 10. 后续版本方向

以下内容保留到后续版本：

独立规则管理能力

基于历史合同的私有规则抽取

企业信息动态注入

更强的行业化规则体系

更复杂的协同与审批流转

这些方向都来自立项书，但建议作为 V1+ 能力分阶段引入，而不是压进当前首版。
